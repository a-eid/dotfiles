alias sudo='sudo env PATH=$PATH'
alias cra='create-react-app'

# django 
alias avenv='source activate'
alias dvenv='source deactivate' 

# list avaiable packages.json scripts
alias lss='ls-scripts'

# listen open ports for listening
alias lsp='lsof -i -n -P | grep LISTEN'
